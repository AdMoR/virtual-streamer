# Requirements for Video Generation Script
# =========================================
# These are additional dependencies needed for the async video generation script

# Configuration management
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Async support
aiohttp>=3.8.0
asyncio>=3.4.3

# LLM providers
anthropic>=0.21.0
openai>=1.12.0
litellm>=1.30.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

# Existing dependencies from requirements.txt (if not already installed)
# These are already in the main requirements.txt but listed here for reference:
# llama-index
# stable-whisper
# opencv-python
# numpy
# torch
# anthropic


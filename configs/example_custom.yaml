# Example Custom Configuration
# =============================
# This is an example of a custom configuration that overrides defaults
# Copy this file and modify it to suit your needs

# Use OpenAI instead of Anthropic
llm:
  provider: openai
  model: gpt-4o
  temperature: 0.5

# Use local Coqui TTS on different port
tts:
  provider: coqui
  host: localhost
  port: 5002

# Use smaller Whisper model for faster processing
stt:
  provider: whisper
  model: tiny

# Video search configuration using remote VideoSearchClient
video_retrieval:
  collection: my_custom_collection  # Qdrant collection name
  server_url: http://localhost:8003  # Video search server URL
  character_filter: fred  # Filter by character tag

# Custom output directory
output_dir: /custom/output/videos
temp_dir: /tmp/video_gen

# More aggressive parallelization
max_parallel_llm_calls: 10

# Longer sentences
max_sentence_length: 50


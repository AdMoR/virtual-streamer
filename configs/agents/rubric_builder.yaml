# Story Generator Agent Configuration
# Generates structured story from title using LLM

name: rubric_builder

model:
  provider: openai
  model: Qwen3-VL-8B-Instruct-GGUF:UD-Q4_K_XL
  parameters:
    temperature: 0.3
    max_output_tokens: 4096
    api_base: http://100.114.182.89:8081

metadata:
  version: "1.0.0"
  description: "Generates Rubrics for a set of stories"
  owners:
    - virtual-streamer-team
  tags:
    - story-generation
    - creative-writing
    - french

transfer_policy:
  allow_transfer: false
  allowed_targets: []

include_contents: true

